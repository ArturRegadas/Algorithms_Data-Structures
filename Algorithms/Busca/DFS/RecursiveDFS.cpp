#include <unordered_map>
#include <vector>
#define MAXN (int)1e4+10
#define umap unordered_map
using namespace std;

umap<int, vector<int>> graph;
vector<int> processed(MAXN, -1);

int main(){

    return 0;
}